/* LCM type definition class file
 * This file was automatically generated by lcm-gen
 * DO NOT MODIFY BY HAND!!!!
 * lcm-gen 1.5.1
 */

package lcmtypes;
 
import java.io.*;
import java.util.*;
import lcm.lcm.*;
 
public final class hopper_data_lcmt implements lcm.lcm.LCMEncodable
{
    /**
     * LCM Type: float[3]
     */
    public float q[];

    /**
     * LCM Type: float[3]
     */
    public float qd[];

    /**
     * LCM Type: float[3]
     */
    public float tauIq[];

    /**
     * ---- RM M2006/C610 motors (via Pixhawk rm_c610 + px4_dds_bridge) ----
     * Output-shaft angle (rad, multi-turn) / speed (rad/s) / torque current (A).
     * rm_online: bit i = ESC (i+1) feedback fresh (<100ms).
     * LCM Type: float[3]
     */
    public float rm_q[];

    /**
     * LCM Type: float[3]
     */
    public float rm_qd[];

    /**
     * LCM Type: float[3]
     */
    public float rm_iq[];

    public byte rm_online;

 
    public hopper_data_lcmt()
    {
        q = new float[3];
        qd = new float[3];
        tauIq = new float[3];
        rm_q = new float[3];
        rm_qd = new float[3];
        rm_iq = new float[3];
    }
 
    public static final long LCM_FINGERPRINT;
    public static final long LCM_FINGERPRINT_BASE = 0x08f00706db6147bcL;
 
    static {
        LCM_FINGERPRINT = _hashRecursive(new ArrayList<Class<?>>());
    }
 
    public static long _hashRecursive(ArrayList<Class<?>> classes)
    {
        if (classes.contains(lcmtypes.hopper_data_lcmt.class))
            return 0L;
 
        classes.add(lcmtypes.hopper_data_lcmt.class);
        long hash = LCM_FINGERPRINT_BASE
            ;
        classes.remove(classes.size() - 1);
        return (hash<<1) + ((hash>>63)&1);
    }
 
    public void encode(DataOutput outs) throws IOException
    {
        outs.writeLong(LCM_FINGERPRINT);
        _encodeRecursive(outs);
    }
 
    public void _encodeRecursive(DataOutput outs) throws IOException
    {
        for (int a = 0; a < 3; a++) {
            outs.writeFloat(this.q[a]); 
        }
 
        for (int a = 0; a < 3; a++) {
            outs.writeFloat(this.qd[a]); 
        }
 
        for (int a = 0; a < 3; a++) {
            outs.writeFloat(this.tauIq[a]); 
        }
 
        for (int a = 0; a < 3; a++) {
            outs.writeFloat(this.rm_q[a]); 
        }
 
        for (int a = 0; a < 3; a++) {
            outs.writeFloat(this.rm_qd[a]); 
        }
 
        for (int a = 0; a < 3; a++) {
            outs.writeFloat(this.rm_iq[a]); 
        }
 
        outs.writeByte(this.rm_online); 
 
    }
 
    public hopper_data_lcmt(byte[] data) throws IOException
    {
        this(new LCMDataInputStream(data));
    }
 
    public hopper_data_lcmt(DataInput ins) throws IOException
    {
        if (ins.readLong() != LCM_FINGERPRINT)
            throw new IOException("LCM Decode error: bad fingerprint");
 
        _decodeRecursive(ins);
    }
 
    public static lcmtypes.hopper_data_lcmt _decodeRecursiveFactory(DataInput ins) throws IOException
    {
        lcmtypes.hopper_data_lcmt o = new lcmtypes.hopper_data_lcmt();
        o._decodeRecursive(ins);
        return o;
    }
 
    public void _decodeRecursive(DataInput ins) throws IOException
    {
        this.q = new float[(int) 3];
        for (int a = 0; a < 3; a++) {
            this.q[a] = ins.readFloat();
        }
 
        this.qd = new float[(int) 3];
        for (int a = 0; a < 3; a++) {
            this.qd[a] = ins.readFloat();
        }
 
        this.tauIq = new float[(int) 3];
        for (int a = 0; a < 3; a++) {
            this.tauIq[a] = ins.readFloat();
        }
 
        this.rm_q = new float[(int) 3];
        for (int a = 0; a < 3; a++) {
            this.rm_q[a] = ins.readFloat();
        }
 
        this.rm_qd = new float[(int) 3];
        for (int a = 0; a < 3; a++) {
            this.rm_qd[a] = ins.readFloat();
        }
 
        this.rm_iq = new float[(int) 3];
        for (int a = 0; a < 3; a++) {
            this.rm_iq[a] = ins.readFloat();
        }
 
        this.rm_online = ins.readByte();
 
    }
 
    public lcmtypes.hopper_data_lcmt copy()
    {
        lcmtypes.hopper_data_lcmt outobj = new lcmtypes.hopper_data_lcmt();
        outobj.q = new float[(int) 3];
        System.arraycopy(this.q, 0, outobj.q, 0, 3); 
        outobj.qd = new float[(int) 3];
        System.arraycopy(this.qd, 0, outobj.qd, 0, 3); 
        outobj.tauIq = new float[(int) 3];
        System.arraycopy(this.tauIq, 0, outobj.tauIq, 0, 3); 
        outobj.rm_q = new float[(int) 3];
        System.arraycopy(this.rm_q, 0, outobj.rm_q, 0, 3); 
        outobj.rm_qd = new float[(int) 3];
        System.arraycopy(this.rm_qd, 0, outobj.rm_qd, 0, 3); 
        outobj.rm_iq = new float[(int) 3];
        System.arraycopy(this.rm_iq, 0, outobj.rm_iq, 0, 3); 
        outobj.rm_online = this.rm_online;
 
        return outobj;
    }
 
}

